<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.seckill.repository.mapper.SeckillGoodsMapper">

    <select id="selectByPrimaryKey" resultType="com.example.seckill.repository.entity.SeckillGoods">
        SELECT id, goods_id, goods_name, seckill_price, original_price,
               total_stock, remaining_stock, status, start_time, end_time,
               create_time, update_time
        FROM seckill_goods
        WHERE id = #{id}
    </select>

    <update id="decreaseStock">
        UPDATE seckill_goods
        SET remaining_stock = remaining_stock - #{quantity}, update_time = NOW()
        WHERE id = #{goodsId} AND remaining_stock >= #{quantity}
    </update>

    <update id="decreaseStockWithVersion">
        UPDATE seckill_goods
        SET remaining_stock = remaining_stock - #{quantity}, update_time = NOW()
        WHERE id = #{goodsId} AND remaining_stock >= #{quantity}
    </update>

    <update id="increaseStock">
        UPDATE seckill_goods
        SET remaining_stock = remaining_stock + #{quantity}, update_time = NOW()
        WHERE id = #{goodsId}
    </update>

</mapper>